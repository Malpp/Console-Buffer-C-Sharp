# Console-Buffer-C-Sharp

add ConsoleBuffer.dll to references

using ConsoleBuffer;

ConsoleBuffer.StartBuffer();

ConsoleBuffer.Write( int left, int top, string );

ConsoleBuffer.ReadLine( int left, int top, string );

ConsoleBuffer.StopBuffer();
